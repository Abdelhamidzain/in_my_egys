import 'package:flutter/material.dart';

class PairCodeEntryScreen extends StatelessWidget {
  const PairCodeEntryScreen({super.key});
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Enter Pair Code')),
      body: Center(child: Text('Coming Soon')),
    );
  }
}
